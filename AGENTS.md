## 프로젝트 참조 순서

코딩 전 반드시 다음 문서를 확인한다.

1. `AGENTS.md`
2. `docs/project-structure.md` — 현재 폴더와 파일의 역할을 확인한다.

GSAP 공식 문서 기반 학습 페이지나 예제를 추가·수정하거나 coverage와 코드 패널 동기화를 검토할 때는 `docs/workflows/README.md`도 읽는다.

## 코딩 행동 지침

LLM이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침. 필요에 따라 프로젝트별 지침과 병합해 사용한다.

**트레이드오프:** 이 지침은 속도보다 신중함 쪽으로 치우쳐 있다. 사소한 작업에는 재량껏 판단한다.

### 1. 코딩하기 전에 생각하라

**추측하지 마라. 혼란을 숨기지 마라. 트레이드오프를 드러내라.**

구현하기 전에:

- 가정을 명시적으로 밝혀라. 확신이 없으면 물어라.
- 해석이 여러 개라면 모두 제시하라 — 말없이 하나를 고르지 마라.
- 더 단순한 방법이 있으면 말하라. 정당한 근거가 있으면 반박하라.
- 불분명한 게 있으면 멈춰라. 무엇이 헷갈리는지 짚어라. 물어라.

### 2. 단순함을 우선하라

**문제를 푸는 최소한의 코드. 추측성 작업은 없다.**

- 요청받은 것 이상의 기능은 없다.
- 한 번 쓰는 코드에 추상화는 없다.
- 요청하지 않은 "유연성"이나 "설정 가능성"은 없다.
- 불가능한 시나리오를 위한 에러 처리는 없다.
- 200줄을 썼는데 50줄로 가능하면, 다시 써라.

스스로 물어라: "시니어 엔지니어가 이걸 과하게 복잡하다고 할까?" 그렇다면, 단순화하라.

### 3. 외과적으로 변경하라

**꼭 필요한 것만 건드려라. 네가 만든 것만 치워라.**

기존 코드를 수정할 때:

- 인접한 코드·주석·포맷을 "개선"하지 마라.
- 고장 나지 않은 것을 리팩터링하지 마라.
- 네 방식과 다르더라도 기존 스타일을 따라라.
- 관련 없는 죽은 코드를 발견하면 언급만 하라 — 지우지 마라.

네 변경이 고아(orphan) 코드를 만들면:

- 네 변경 때문에 안 쓰이게 된 import·변수·함수를 제거하라.
- 원래 있던 죽은 코드는 요청받지 않는 한 제거하지 마라.

기준: 바뀐 모든 줄은 사용자의 요청으로 곧장 추적될 수 있어야 한다.

### 4. 목표 주도로 실행하라

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

작업을 검증 가능한 목표로 바꿔라:

- "검증을 추가하라" → "확인할 동작과 재현 절차를 정의하고, 직접 확인하라"
- "버그를 고쳐라" → "증상을 재현하고, 수정 후 같은 절차로 사라졌는지 확인하라"
- "X를 리팩터링하라" → "전후로 빌드와 관련 화면 동작이 유지되는지 확인하라"

여러 단계로 이뤄진 작업은 간단한 계획을 밝혀라:

```
1. [단계] → 검증: [확인]
2. [단계] → 검증: [확인]
3. [단계] → 검증: [확인]
```

강한 성공 기준은 스스로 반복하며 일하게 해준다. 약한 기준("그냥 되게 만들어")은 끊임없는 재확인을 요구한다.

### 5. 테스트 코드를 작성하지 마라

**이 프로젝트에서는 자동화 테스트 코드를 작성하거나 테스트 실행 환경을 추가하지 않는다.**

- 변경은 TypeScript·Vite 빌드, Storybook 빌드, 브라우저 동작으로 검증한다.
- `*.test.*`, 테스트 전용 디렉터리, 테스트 러너 설정과 테스트 전용 의존성을 추가하지 않는다.

### 6. 주석은 한 줄로 단다

**이 프로젝트를 처음 보는 개발자가 그 한 줄만 읽고 바로 이해할 수 있게 쓴다.**

- 위치와 형식: 파일 맨 위와 모든 `export` 위에 `/** ... */` 한 줄, 함수 안 판단이 필요한 줄 위에 `//` 한 줄. 본문은 한국어, 식별자는 영어.
- 내용: 코드에 이미 적힌 문법 말고, **이게 무슨 역할이고 왜 이렇게 했는지**를 쓴다.
  - ❌ `/** pathname을 split해서 ResolvedRoute를 반환한다 */`
  - ✅ `/** 모르는 경로도 첫 레슨으로 흡수해 404 화면 없이 학습을 잇는다 */`
- 한 줄에 안 들어가면 주석이 아니라 코드를 고칠 신호다 — 함수를 쪼개거나 이름을 바꾼다.
- 여러 줄 블록과 `@param`/`@returns`는 쓰지 않는다. 모르면 사고 나는 것(보안·브라우저 호환)만 예외로 두 줄까지 허용한다.
- 안 다는 곳: 다시 내보내기만 하는 `index.ts`, 읽으면 바로 아는 줄. `export`가 하나뿐인 파일은 맨 위 한 줄로 갈음한다.
- `examples/` 아래는 GSAP 문법 설명 자체가 학습 목적이므로 문법을 풀어 써도 된다.

### 7. GSAP 학습 코드는 단계마다 주석을 단다

**대상: `examples/` 아래의 예제 컴포넌트와 `use*Animation.ts` 훅.** 여기서는 코드가 곧 교재이므로, 위에서 아래로 읽기만 해도 애니메이션이 어떻게 조립되는지 따라올 수 있어야 한다. 나머지 파일은 6번 규칙만 따른다.

- **선언마다 한 줄** — `ref`, `useState`, 상수, config 객체 등 모든 선언 위에 `//` 한 줄로 *이 값이 GSAP에서 맡는 역할*을 쓴다. 타입 별칭과 config 타입도 포함한다.
- **실행 단계마다 한 줄** — `useGSAP` 콜백 안의 `gsap.set` / `gsap.to` / timeline 체인은 실행 순서대로 각 줄 위에 *이 단계에서 화면에 무슨 일이 일어나는지*를 쓴다.
- **옵션 객체 한 줄** — `useGSAP`의 `{ scope, dependencies, revertOnUpdate }`처럼 역할이 섞인 옵션은 그 줄 위에 한 줄로 왜 그 조합인지 쓴다.
- **반환 객체는 한 줄로 갈음** — 각 키는 이미 위에서 설명했으므로 소비처가 무엇을 받는지만 한 줄로 쓴다.
- 길이는 6번과 같이 **한 줄 이내**, 한국어로 쓴다. 두 줄이 필요하면 값을 쪼개거나 이름을 바꾼다.

```ts
// 애니메이션이 적용될 DOM 범위 — useGSAP이 이 안에서만 선택자를 찾고 정리한다
const scope = useRef<HTMLDivElement>(null)
// gsap.to에 넘길 선택자이자 JSX className — 실행과 표시가 같은 문자열을 쓴다
const targetClassName = 'keyframes-example__target'
// 배열형 keyframes와 퍼센트형 keyframes 중 무엇을 실행할지 결정한다
const [mode, setMode] = useState<KeyframeMode>('array')

useGSAP(
  () => {
    // 재생 전 이전 tween이 남긴 transform을 지워 항상 같은 지점에서 출발시킨다
    gsap.set(`.${targetClassName}`, { x: 0, y: 0, rotation: 0, scale: 1 })
    // keyframes를 순서대로 이어 붙여 하나의 tween으로 재생한다
    gsap.to(`.${targetClassName}`, animationConfig)
  },
  // mode·모션 설정·replay 중 하나만 바뀌어도 이전 tween을 되돌리고 처음부터 다시 만든다
  { scope, dependencies: [mode, reducedMotion, runKey], revertOnUpdate: true },
)
```

---

**이 지침이 잘 작동하고 있다는 신호:** diff에 불필요한 변경이 줄고, 과한 복잡함 때문에 다시 쓰는 일이 줄며, 명확화 질문이 실수 뒤가 아니라 구현 전에 나온다.
