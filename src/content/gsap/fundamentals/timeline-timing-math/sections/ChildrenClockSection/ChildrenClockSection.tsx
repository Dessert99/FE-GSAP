/** Timeline raw 길이가 children에서 나오고 자신의 delay는 부모 startTime에만 반영됨을 설명한다. */
import { SectionHeading } from '../../components/SectionHeading/SectionHeading'
export function ChildrenClockSection(){return <section id="children-clock" className="tl-timing-page__section" aria-labelledby="children-clock-title"><SectionHeading number="01" id="children-clock" title="길이를 정하는 건 children이다" description="빈 Timeline은 0초이고 child의 배치·delay·길이가 raw duration을 만든다. Timeline 자신의 delay는 부모 위 시작점만 옮긴다."/><pre className="tl-timing-page__code"><code>{`delay( value:Number ) : [Number | self]
const currentDelay = timeline.delay()
timeline.delay(2)`}</code></pre><div className="tl-timing-page__split"><div className="tl-timing-page__prose"><p>delay는 시작 전 기다리는 초이며 읽고 쓸 수 있습니다. 시작값은 delay가 끝날 때 기록되지만 from()은 기본 immediateRender true이고 false로 막을 수 있습니다.</p></div><div className="tl-timing-page__prose"><p>공식은 delay가 timeScale 영향은 받지 않는다고 밝힙니다. 속도를 10배로 바꿔도 설정된 delay 숫자는 그대로입니다.</p></div></div><div className="tl-timing-page__note tl-timing-page__note--probe"><p>빈 Timeline duration은 0, child 추가·제거에 따라 변했습니다. child delay와 position은 더해지고 같은 위치의 child는 최대 끝을 넘지 않으면 늘리지 않습니다. Timeline 자신의 delay는 duration/totalDuration이 아니라 startTime에만 들어갔습니다.</p></div></section>}
