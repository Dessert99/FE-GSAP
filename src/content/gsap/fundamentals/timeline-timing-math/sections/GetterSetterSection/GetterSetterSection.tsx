/** 다섯 getter/setter의 NaN 기본 표기와 인자 유무에 따른 Number/self 반환을 비교한다. */
import { SectionHeading } from '../../components/SectionHeading/SectionHeading'
const methods=[['delay','Number'],['duration','Number'],['totalDuration','Number'],['startTime','Number'],['timeScale','Number']] as const
export function GetterSetterSection(){return <section id="getter-setter" className="tl-timing-page__section" aria-labelledby="getter-setter-title"><SectionHeading number="02" id="getter-setter" title="다섯 개가 전부 같은 모양이다" description="인자를 생략하면 숫자 getter, Number를 넘기면 self를 돌려주는 setter다. 공식 표의 value 기본값은 모두 NaN이다."/><div className="tl-timing-page__table-wrap"><table className="tl-timing-page__rules-table"><caption>공통 getter/setter 계약</caption><thead><tr><th>메서드</th><th>value</th><th>getter</th><th>setter</th></tr></thead><tbody>{methods.map(([name,type])=><tr key={name}><th><code>{name}(value)</code></th><td>{type} · default NaN</td><td>현재 Number</td><td>self</td></tr>)}</tbody></table></div><div className="tl-timing-page__warning"><p><code>totalDuration</code> value 설명에만 음수는 animation END에서부터 해석된다는 문장이 붙어 있습니다. 다른 네 페이지에는 없습니다.</p></div><pre className="tl-timing-page__code"><code>{`timeline.delay(2).timeScale(0.5).play(1)
timeline.duration(2).play(1)`}</code></pre></section>}
