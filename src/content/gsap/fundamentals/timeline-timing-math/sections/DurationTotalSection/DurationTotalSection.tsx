/** repeat을 제외한 duration과 포함한 totalDuration의 공식 예제와 실행 식을 구분한다. */
import { SectionHeading } from '../../components/SectionHeading/SectionHeading'
export function DurationTotalSection(){return <section id="duration-total" className="tl-timing-page__section" aria-labelledby="duration-total-title"><SectionHeading number="03" id="duration-total" title="duration과 totalDuration이 갈리는 곳" description="한 cycle 길이는 duration, repeat·repeatDelay까지 포함한 전체 길이는 totalDuration이다."/><div className="tl-timing-page__split"><div className="tl-timing-page__prose"><pre className="tl-timing-page__code"><code>{'duration( value:Number ) : [Number | self]'}</code></pre><p>duration 2, repeat 3이면 raw duration은 2이고 totalDuration은 8입니다.</p></div><div className="tl-timing-page__prose"><pre className="tl-timing-page__code"><code>{'totalDuration( value:Number ) : [Number | self]'}</code></pre><p>duration 10, repeat 1, repeatDelay 2면 totalDuration은 22입니다.</p></div></div><div className="tl-timing-page__note tl-timing-page__note--probe"><h3>실행으로 확인한 식</h3><p><code>totalDuration = duration × (repeat + 1) + repeatDelay × repeat</code>. repeat -1의 getter는 Infinity가 아니라 <strong>10000000000</strong>입니다.</p></div><pre className="tl-timing-page__code"><code>{`const duration = timeline.duration()
const total = timeline.totalDuration()
timeline.duration(10)
timeline.totalDuration(10)`}</code></pre></section>}
