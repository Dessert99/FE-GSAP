/** 중첩 startTime과 timeScale을 풀어 local time을 globalTimeline 좌표로 변환하는 규칙을 설명한다. */
import { SectionHeading } from '../../components/SectionHeading/SectionHeading'
export function GlobalTimeSection(){return <section id="global-time" className="tl-timing-page__section" aria-labelledby="global-time-title"><SectionHeading number="07" id="global-time" title="중첩을 다 풀어 전역 시계로" description="globalTime(localTime)은 모든 parent startTime과 timeScale을 반영해 gsap.globalTimeline의 대응 Number를 돌려준다."/><pre className="tl-timing-page__code"><code>{`globalTime( localTime:Number ) : Number
timeline.globalTime(0)`}</code></pre><div className="tl-timing-page__prose"><p>중첩 Timeline 시작 0이 전역의 어디인지 묻는 메서드입니다. 공식은 인자 생략 시 자신의 totalTime을 사용해 <code>globalTime(totalTime())</code>과 같다고 설명합니다.</p></div><div className="tl-timing-page__note tl-timing-page__note--probe"><p>실행 계산은 자기부터 parent로 올라가며 <code>time = startTime + time ÷ |timeScale|</code>을 반복했습니다.</p></div><div className="tl-timing-page__warning"><p>GSAP 3.15.0에서 인자 없는 호출은 totalTime이 아니라 playhead 기준 rawTime을 사용했습니다. 부모 playhead가 이 Timeline 위에 없으면 공식 등식이 성립하지 않습니다. 타입 선언에도 globalTime이 없어 호출 지점에서만 좁힌 로컬 타입이 필요합니다.</p></div></section>}
