/** duration 계열 setter가 children 대신 Timeline timeScale과 부모 배정 폭을 바꾸는 점을 실험한다. */
import { DurationFitLab } from '../../examples/DurationFitLab/DurationFitLab'
import { SectionHeading } from '../../components/SectionHeading/SectionHeading'
export function SetterScalesSection(){return <section id="setter-scales" className="tl-timing-page__section" aria-labelledby="setter-scales-title"><SectionHeading number="04" id="setter-scales" title="setter는 길이가 아니라 배속을 바꾼다" description="Timeline의 raw 길이는 contents가 결정하므로 duration(v)·totalDuration(v)는 지정 폭에 맞도록 자기 timeScale만 조정한다."/><div className="tl-timing-page__warning"><p>20초 children에 <code>duration(10)</code>을 호출하면 timeScale은 2가 되지만 다시 <code>duration()</code>을 읽으면 20입니다. children은 변하지 않습니다.</p></div><div className="tl-timing-page__note tl-timing-page__note--probe"><p>duration(v) 새 timeScale은 <code>totalDuration ÷ (v × (repeat+1) + repeatDelay×repeat)</code>, totalDuration(v)는 <code>totalDuration ÷ v</code>였습니다. child를 이후 추가·제거해도 timeScale은 남아 맞춘 폭이 깨지고, 빈 Timeline에 setter를 부르면 timeScale은 0이 됩니다.</p></div><DurationFitLab/></section>}
