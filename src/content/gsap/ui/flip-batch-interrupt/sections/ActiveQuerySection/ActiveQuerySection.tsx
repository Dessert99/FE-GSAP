/** target-scoped active Boolean query를 설명한다. */
export function ActiveQuerySection() {
  return (
    <section>
      <h2>03 · active query</h2>
      <p>
        <code>Flip.isFlipping(target)</code>은 stable Element 또는 selector
        하나의 active Boolean을 읽습니다.
      </p>
    </section>
  )
}
