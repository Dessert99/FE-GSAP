/** P46의 three owned integration canonical을 exact audit 순서로 제공한다. */
export const scrollTriggerIntegrationsCatalog = [
  [
    'STI-194',
    'normalizeScroll boolean/object getter-setter, Observer-backed JS-thread normalization and disable',
  ],
  [
    'STI-195',
    'observe config returns Observer and is functionally Observer.create equivalent',
  ],
  [
    'STI-200',
    'scrollerProxy supplies required scroll getter/setter and optional measurement/pin methods',
  ],
] as const
